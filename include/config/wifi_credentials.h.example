#pragma once

// Copy this file to wifi_credentials.h (already gitignored) and fill in
// your network's details. wifi_credentials.h is never committed.

#define WIFI_SSID "your-network-name"
#define WIFI_PASSWORD "your-network-password"
